#include LAU.h

public void turn() {
	typedef char bool;
	#define left 'l'
	#define right 'r'
	bool locationFromTape = left;

	int leftMargin = 0;
	int rightMargin = 200;
		int centerMargin = rightMargin / 2;

	if (lightValue )


}